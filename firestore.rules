rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개선노트: 개인용 앱, 익명 로그인만 요구 (완전한 사용자 구분은 없음)
    match /photoNotes/{noteId} {
      allow read, write: if request.auth != null;
    }
  }
}
