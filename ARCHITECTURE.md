# 여행앱 구조 (사진+여행 통합 PWA)

마지막 갱신: 2026-08-22 — 사진앱 기능 통합 완료 (index.html에 실제 반영됨)

## 구현 결과 요약
- 최상위 탭은 `📷 사진` / `🧭 여행` 2개 (`.tab`, `#view-photo` / `#view-travel`).
- `📷 사진` 안의 2차 서브탭(`#photo-nav`): 설정 계산기 / S·S 사용법(외부 링크) / 가이드 자료(외부 링크 목록) / 내 사진(갤러리).
- `🧭 여행` 안의 2차 서브탭(`#travel-nav`): 등산 / 국내 / 해외 / 골프 — 각각 기존 3차 서브탭(예: 등산의 1h/2h/3h) 구조를 그대로 유지.
- 등산·국내·해외·골프 카드 하단에 "📷 촬영 설정 보기" 버튼 → `goToPhotoCalc(major, minor)` 호출로 사진 탭 계산기로 전환 + 대상 값 프리셋.
- 전체 색상 토큰을 사진앱의 다크 테마로 통일 (`--bg:#161619` 등, index.html `<style> :root`).

## 저장소(레포) 2개로 분리 운영 — 절대 하나로 합치지 않음

| 레포 | 역할 | 배포 |
|---|---|---|
| **hiking-list** (지금 이 폴더/레포) | 메인 앱. 사진 계산기+갤러리, 등산/국내/해외 코스 | GitHub Pages (main 브랜치) |
| **camera-guide** (별도 폴더 `사진앱/`, 별도 레포) | 촬영 가이드 PDF 11개 + Snapseed 가이드 PDF 원본·호스팅 전용 | GitHub Pages (main 브랜치), URL은 `https://cennong1412.github.io/camera-guide/` 로 추정 — 실제 반영 전 최초 1회 브라우저로 확인 필요 |

**이유**: guide PDF 전체가 약 140MB라 hiking-list 레포로 그대로 옮기면 저장소가 과도하게 무거워짐. 그래서 PDF는 camera-guide에 그대로 두고, hiking-list의 "가이드 자료" 메뉴에서 외부 링크(새 탭)로만 연결한다.

**유지보수 규칙**:
- 가이드 PDF 내용을 고치고 싶다 → `사진앱/`(camera-guide 레포)에서 작업 후 그 레포만 커밋·푸시. 여행앱 레포는 안 건드림.
- 촬영 설정 계산기 로직 / 내 사진 갤러리 / 여행 코스를 고치고 싶다 → `여행앱/`(hiking-list 레포)의 `index.html`만 수정.
- 두 앱은 디자인 테마(다크, 사진앱 스타일)만 통일했을 뿐 코드와 배포는 완전히 독립 — 한쪽을 고쳐도 다른 쪽에 영향 없음.

## hiking-list 레포 파일 구조 (통합 후)

```
여행앱/
├── index.html                        # 앱 전체. 최상위 탭: 📷 사진 / 🧭 여행
├── manifest.json / service-worker.js # PWA 설정, 캐시
├── domestic_courses_all.json         # 국내 코스 데이터
├── international_courses_all.json    # 해외 코스 데이터
├── golf_courses_all.json             # 골프 코스 데이터
├── photos/
│   ├── manifest.json                 # 카테고리별 사진 목록 (사진앱에서 이식)
│   ├── thumbs/                       # 썸네일 (build-photos.js가 자동 생성)
│   └── (카테고리 폴더들, 실사진은 추후 추가)
├── scripts/build-photos.js           # 사진 등록 시 썸네일+manifest 갱신 스크립트 (사진앱에서 이식)
├── package.json                      # build-photos 스크립트용 (npm install 후 npm run build-photos)
├── icons/                            # 여행앱 자체 PWA 아이콘 (사진앱 아이콘은 안 가져옴)
└── ARCHITECTURE.md                   # 이 문서
```

camera-guide 레포(`사진앱/` 폴더)는 이번 통합으로 **내용이 바뀌지 않음** — 그대로 독립 운영.

## 최상위 네비게이션 설계

- **📷 사진**
  - 설정 계산기 — 사진앱의 계산 로직을 그대로 이식(대상/환경/목적/의도/안정화/기법 선택 → 추천)
  - 내 사진 — 갤러리 (`photos/manifest.json` 기반, 카테고리 탭)
  - 가이드 자료 — camera-guide 배포 사이트로 외부 링크(새 탭 열기), 여행앱 레포에는 PDF 없음
- **🧭 여행**
  - ⛰️ 등산 / 🇰🇷 국내 / ✈️ 해외 — 기존 서브탭 구조 그대로 유지, 색상만 다크 테마로 교체

## 코스 ↔ 사진 연동

등산/국내/해외 각 코스 카드에 "📷 촬영 설정 보기" 버튼을 추가. 클릭 시 최상위 탭이 📷 사진으로 전환되고 설정 계산기의 "대상" 선택값이 코스 성격에 맞게 자동 지정된다(예: 등산 코스 → 대상=풍경/산·바다).

## 테마

기존 여행앱의 밝은 베이지 톤 대신, 사진앱의 다크 테마(`--bg:#161619`, `--card:#1f1f24`, `--accent:#e0a13c` 등)로 앱 전체 색상을 통일한다.
